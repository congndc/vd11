@model IEnumerable<vd11.Models.ThamNien>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ThamNienLam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Luong)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ThamNienLam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Luong)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ThamNienID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ThamNienID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ThamNienID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
